# Database
DATABASE_URL="file:./dev.db"

# Google Analytics
NEXT_PUBLIC_GA4_ID="G-XXXXXXX"

# Stripe
STRIPE_SECRET_KEY="sk_live_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# Stripe Price IDs for Premium Provider Tiers
# Create these in your Stripe Dashboard as recurring prices
STRIPE_PRICE_FOUNDING_PARTNER="price_fp_49_monthly"    # $49/month - Founding Partner Premium
STRIPE_PRICE_STANDARD_PREMIUM="price_sp_79_monthly"    # $79/month - Standard Premium Listing
STRIPE_PRICE_HIGH_DENSITY="price_hd_149_monthly"       # $149/month - High-Density Metro Tier

# Twilio
TWILIO_ACCOUNT_SID="ACxxxxxxxx"
TWILIO_AUTH_TOKEN="xxxxxxxx"
TWILIO_MESSAGING_SERVICE_SID="MGxxxxxxxx"
TWILIO_VOICE_WEBHOOK_URL="https://yourdomain.com/api/telephony/voice"
TWILIO_STATUS_WEBHOOK_URL="https://yourdomain.com/api/telephony/status"

# SendGrid
SENDGRID_API_KEY="SG.xxxxxxxx"

# Site Configuration
PUBLIC_SITE_URL="https://mobilephlebotomy.org"
NEXT_PUBLIC_SITE_URL="https://mobilephlebotomy.org"
NEXT_PUBLIC_DEFAULT_PHONE="1-800-555-0100"  # Default tracking number

# Email Configuration
LEAD_EMAIL_FROM="noreply@mobilephlebotomy.org"
ADMIN_EMAIL="hector@mobilephlebotomy.org"

# Client Order Portal
# HMAC key for signing client_session cookies. REQUIRED — without it
# /orders/verify throws a 500 the moment a client clicks their magic link
# (the token is already consumed by then, so they must request a new one).
# Use a dedicated value, NOT ADMIN_SECRET. Generate: openssl rand -base64 32
CLIENT_SESSION_SECRET="base64-32-bytes"

# Node Environment
NODE_ENV="production"
